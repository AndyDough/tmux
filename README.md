1. Manual install of tmux
- where to clone config and tpm
```
git clone git@github.com/AndyDough/tmux.git ~/.config/tmux
git clone https://github.com/tmux-plugins/tpm ~/.config/tmux/plugins/tpm
```
- prefix + I to install plugins with tpm

